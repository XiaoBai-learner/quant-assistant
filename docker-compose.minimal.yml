# 最小化 Docker Compose - 仅测试应用 (无MySQL)
version: '3.8'

services:
  quant:
    build: .
    container_name: quant_test
    environment:
      # SQLite 模式 (无需MySQL)
      USE_SQLITE: "true"
    volumes:
      - ./:/app
    command: python test_mock.py
